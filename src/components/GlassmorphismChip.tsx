const GlassmorphismChip = () => {
  return (
    <div className="relative inline-flex items-center px-4 py-2 rounded-full animate-border-glow border border-glass-border/20 text-glass-text text-xs font-body font-medium mb-6">
      <span className="relative z-10">Beta is rolling out soon • Signup for early access</span>
    </div>
  );
};

export default GlassmorphismChip;