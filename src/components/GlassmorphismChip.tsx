const GlassmorphismChip = () => {
  return (
    <div className="inline-flex items-center px-3 py-1.5 rounded-full backdrop-blur-md bg-glass-bg/10 border border-glass-border/20 text-glass-text text-xs font-body font-medium mb-6">
      Beta is rolling out soon • Signup for early access
    </div>
  );
};

export default GlassmorphismChip;